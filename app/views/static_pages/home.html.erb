<% provide(:title, 'Home') %>
<div class="center hero-unit">
  <% if Setting.exists? %>
    <%= raw RedCloth.new(Setting.first.mainimageurl).to_html %>
  <% end %>
  <h1>
    <% if Setting.exists? %>
      <%= Setting.first.logostring %>
    <% else %>
      Empty
    <% end %>
  </h1>
  <blockquote>
	  <h3>
      <% if Setting.exists? %>
        <%= Setting.first.maindescription %>
      <% else %>
        <% if signed_in? %>
        <%= link_to "Setting Your pages.", settings_path %>
        <% else %>
          Sign up and Sign in -> Make your pages.
        <% end %> 
      <% end %>
	  </h3>
	</blockquote>
  <% if signed_in? %>
    <%= current_user.name  %> Welcome.
  <% else %>
    <%= link_to "가입하기", signup_path, class: "btn btn-smmll btn-primary"%>
  <% end %>
</div>
<div class = "row">
  <div class="span12">
    <div class="span8">
      <table class="<%= cycle("odd", "even") %>">
      <% if Forum.exists? %>
        <h3 class="center"><%=Forum.first.title %></h3>
        <% Forum.first.topics.each do |topic| %>
          
          <% if topic.image.exists? %>
            <tr>
              <td><h3><%= topic.title %></h3></td><br/>
            </tr>
            <tr>
              <td class="span3"><%= image_tag topic.image, class:"thumbnail" %></td>
              <td class="span5 offset1 topicdescription"><h4>  <%= topic.description%></h4></td>
            </tr><br/>
          <% end %>
        <% end %>
      <% else %>
      <% end %>
      </table>
    </div>
    <div class="span3">  
      <% @topics =Forum.find(:first,order:"id",offset: 1) %>
      <table>
        <h3 class ="center"><%=@topics.title%></h3>
        <% @topics.topics.each do |topic| %>
          <tr>
            <td><%= topic.title %></td>
          </tr>
          <tr>
            <td><%= topic.description %></td>
          </tr>
        <% end %>
      </table>

    </div>
  </div>
</div>